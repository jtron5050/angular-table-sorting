import { TableSort } from './table-sort.directive';

describe('TableSortDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSort();
    expect(directive).toBeTruthy();
  });
});
